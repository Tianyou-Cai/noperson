<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cty.nopersonfinally.mapper.AnnouncementMapper">

    <sql id="Base_Column_List">
        announcement_id, title, content, target_role, is_active, create_time, update_time
    </sql>

    <select id="selectPageList" resultType="com.cty.nopersonfinally.pojo.entity.Announcement">
        SELECT <include refid="Base_Column_List" />
        FROM announcement
        WHERE 1=1
        <if test="title != null and title != ''">
            AND title LIKE CONCAT('%', #{title}, '%')
        </if>
        <if test="isActive != null">
            AND is_active = #{isActive}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectActiveList" resultType="com.cty.nopersonfinally.pojo.entity.Announcement">
        SELECT <include refid="Base_Column_List" />
        FROM announcement
        WHERE is_active = 1
        AND (target_role = 'ALL' OR target_role = #{targetRole})
        ORDER BY create_time DESC
    </select>

</mapper>